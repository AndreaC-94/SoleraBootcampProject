function ControlDisplay() {
    let mainVisible = true;

    function showMainView(boolean) {
        mainVisible = boolean;
    }

    function showGroupView(boolean) {
        if(boolean === true) {
            
        }
        

        
    }
}